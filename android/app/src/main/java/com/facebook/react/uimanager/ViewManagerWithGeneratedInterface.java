package com.facebook.react.uimanager;

import android.view.View;

/**
 * Stubbed interface to satisfy libraries that reference codegen-generated view manager contracts.
 * React Native 0.73 no longer ships this type, but some dependencies still extend it.
 */
public interface ViewManagerWithGeneratedInterface<T extends View> {
}
